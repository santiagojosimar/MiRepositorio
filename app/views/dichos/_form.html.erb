<%= form_for(@dicho) do |f| %>
  <% if @dicho.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dicho.errors.count, "error") %> prohibited this dicho from being saved:</h2>

      <ul>
      <% @dicho.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :dicho %><br />
    <%= f.text_area :dicho %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
